# HTTP / HTTPS — TCP and UDP (covers HTTP/3 QUIC on UDP 443)
add rule inet filter input tcp dport { 80, 443 } ct state new accept comment "allow HTTP/HTTPS TCP"
add rule inet filter input udp dport { 80, 443 } ct state new accept comment "allow HTTP/HTTPS UDP (QUIC)"
